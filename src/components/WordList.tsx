import React from "react";
import { Word } from "components/Word";

interface IWordList {
  text: string;
}

const WordList: React.FC<IWordList> = ({ text }) => {
  return <></>;
};

export { WordList };
